= simple_form_for @lab_test_type do |f|
  -if @lab_test_type.errors.any?
    #error_explanation
      %h2= "#{pluralize(@lab_test_type.errors.count, "error")} prohibited this lab_test_type from being saved:"
      %ul
        - @lab_test_type.errors.full_messages.each do |msg|
          %li= msg

  = f.input :name,:label => "Тип анализа"
  = f.submit 'Сохранить'

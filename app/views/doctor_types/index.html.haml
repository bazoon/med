%h2 Типы врачей
%br


-unless flash.empty?
  %p#notice= notice

%table#newspaper-a
  %tr
    %th Тип
    %th
    %th

  - @doctor_types.each do |doctor_type|
    %tr
      %td= doctor_type.name
      %td= link_to image_tag("",:class => "ui-icon ui-icon-pencil"), edit_doctor_type_path(doctor_type)
      %td= link_to image_tag("",:class => "ui-icon ui-icon-minus"), doctor_type, :confirm => 'Are you sure?', :method => :delete if doctor_type.can_be_deleted
%br

= link_to 'Добавить врача', new_doctor_type_path

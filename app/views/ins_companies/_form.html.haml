= simple_form_for @ins_company do |f|
  -if @ins_company.errors.any?
    #error_explanation
      %h2= "#{pluralize(@ins_company.errors.count, "error")} prohibited this ins_company from being saved:"
      %ul
        - @ins_company.errors.full_messages.each do |msg|
          %li= msg
  =f.input :name
  =f.submit "Сохранить"

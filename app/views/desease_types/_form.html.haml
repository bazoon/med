= simple_form_for @desease_type do |f|
  -if @desease_type.errors.any?
    #error_explanation
      %h2= "#{pluralize(@desease_type.errors.count, "error")} prohibited this desease_type from being saved:"
      %ul
        - @desease_type.errors.full_messages.each do |msg|
          %li= msg

  = f.input :name,:label => "Тип заболевания"
  = f.submit 'Сохранить'
